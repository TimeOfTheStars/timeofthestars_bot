# 🏒 Time of the Stars - Telegram Бот Хоккейной Лиги

Telegram-бот для автоматизации работы любительской хоккейной лиги "Time of the Stars". Предоставляет информацию о матчах, отправляет уведомления о предстоящих играх и позволяет командам и игрокам регистрироваться в лиге.

## 📋 Возможности

### Для болельщиков
- 📅 **Просмотр расписания матчей** - информация о предстоящих играх
- 🔔 **Автоматические уведомления** - напоминания о матчах за N часов до начала
- 📊 **Турнирная таблица** - просмотр положения команд в лиге
- 🏆 **Статистика игроков** - рейтинг лучших игроков сезона
- ➡️ **Постраничная навигация** - просмотр следующих матчей блоками по 3 штуки

### Для команд
- 👥 **Подача заявок** - регистрация команды для участия в лиге
- ✏️ **Управление заявками** - редактирование и удаление поданных заявок
- 📋 **Просмотр статуса** - отслеживание статуса рассмотрения заявки (на рассмотрении/одобрена/отклонена)

### Для игроков
- 👤 **Регистрация игрока** - создание анкеты свободного агента
- 🎯 **Выбор команды** - указание предпочитаемой команды для игры
- 📝 **Заполнение профиля** - информация об опыте, позиции, контактных данных
- 🔄 **Редактирование анкеты** - обновление личных данных

### Для администраторов
- 🔐 **Веб-панель SQLAdmin** - удобное управление всеми данными через браузер
- 👥 **Управление пользователями** - просмотр подписчиков на уведомления
- 📋 **Обработка заявок** - одобрение/отклонение заявок команд с комментариями
- 🔑 **Система ролей** - администраторы (полные права) и менеджеры (без удаления)
- 📊 **История уведомлений** - отслеживание отправленных оповещений
- 🎯 **Управление игроками** - просмотр и редактирование анкет игроков

## 🏗️ Архитектура проекта

```
timeofthestars_bot/
├── bot.py                    # Главный файл запуска бота
├── config.py                 # Конфигурация и настройки
├── requirements.txt          # Зависимости проекта
├── start.sh                 # Скрипт одновременного запуска бота и админки
├── create_admin.py          # Утилита управления администраторами
├── run_admin.py             # Запуск админ-панели
│
├── database/                # Слой работы с БД
│   ├── __init__.py
│   ├── database.py          # Подключение и управление сессиями
│   ├── models.py            # ORM модели (User, Player, TeamApplication и др.)
│   └── migrate_db.py        # Миграции базы данных
│
├── handlers/                # Обработчики команд и сообщений бота
│   ├── __init__.py
│   ├── start.py             # Команда /start и главное меню
│   ├── notifications.py     # Матчи и уведомления
│   ├── team_registration.py # Регистрация команд
│   └── player_registration.py # Регистрация игроков
│
├── keyboards/               # Telegram клавиатуры
│   ├── __init__.py
│   └── reply_keyboards.py   # Reply-клавиатуры для навигации
│
├── admin/                   # Админ-панель
│   ├── __init__.py
│   └── admin_panel.py       # Настройка SQLAdmin с аутентификацией
│
└── utils/                   # Вспомогательные модули
    ├── __init__.py
    ├── api_service.py       # Работа с API лиги
    ├── scheduler.py         # Планировщик уведомлений
    └── helpers.py           # Вспомогательные функции
```

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- pip (менеджер пакетов Python)
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/TimeOfTheStars/timeofthestars_bot.git
cd timeofthestars_bot
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv .venv
source .venv/bin/activate  # для Linux/macOS
# или
.venv\Scripts\activate  # для Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env` с настройками:**
```env
# Telegram Bot
BOT_TOKEN=your_bot_token_here

# External API
API_TEAMS=https://api.timeofthestars.ru/teams
API_GAMES=https://api.timeofthestars.ru/games

# Database
DATABASE_URL=sqlite:///hockey_league.db

# SQLAdmin
ADMIN_SECRET_KEY=your-secret-key-change-in-production
ADMIN_PORT=5000
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Уведомления (за сколько часов до игры отправлять)
NOTIFICATION_HOURS_BEFORE=2
```

5. **Создайте первого администратора:**
```bash
python create_admin.py
```
Выберите вариант 1 и следуйте инструкциям.

### Запуск

#### Вариант 1: Запуск всех сервисов одной командой
```bash
chmod +x start.sh
./start.sh
```

#### Вариант 2: Раздельный запуск

**Только бот:**
```bash
python bot.py
```

**Только админ-панель:**
```bash
python run_admin.py
```
Админ-панель будет доступна по адресу: http://localhost:5000/admin

## 🔧 Настройка

### Переменные окружения

| Переменная | Описание |
|-----------|----------|
| `BOT_TOKEN` | Токен Telegram бота |
| `API_TEAMS` | URL API для получения команд |
| `API_GAMES` | URL API для получения игр |
| `DATABASE_URL` | URL подключения к БД |
| `ADMIN_SECRET_KEY` | Секретный ключ для сессий админки |
| `ADMIN_PORT` | Порт для админ-панели |
| `NOTIFICATION_HOURS_BEFORE` | За сколько часов до игры отправлять уведомление |

### База данных

По умолчанию используется SQLite. Для использования PostgreSQL/MySQL измените `DATABASE_URL`:

```env
# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost/dbname

# MySQL
DATABASE_URL=mysql://user:password@localhost/dbname
```

## 📊 Модели данных

### User (Пользователи)
- `telegram_id` - ID пользователя в Telegram
- `notifications_enabled` - Включены ли уведомления
- `created_at` - Дата регистрации

### TeamApplication (Заявки команд)
- `telegram_id` - ID подавшего заявку
- `team_name` - Название команды
- `captain_name` - ФИО капитана
- `captain_phone` - Телефон для связи
- `city` - Город команды
- `description` - Описание команды
- `status` - Статус заявки (pending/approved/rejected)
- `admin_comment` - Комментарий администратора

### Player (Игроки)
- `telegram_id` - ID игрока в Telegram
- `username` - Username в Telegram
- `full_name` - ФИО игрока
- `birth_year` - Год рождения
- `position` - Позиция (forward/defender/goalie)
- `experience` - Опыт игры
- `phone` - Контактный телефон
- `preferred_team_slug` - Предпочитаемая команда

### GameNotification (История уведомлений)
- `game_id` - ID игры из API
- `notified_at` - Время отправки уведомления
- `users_count` - Количество уведомленных пользователей

### Admin (Администраторы)
- `username` - Логин администратора
- `password_hash` - Хэш пароля (SHA-256)
- `full_name` - ФИО администратора
- `role` - Роль (admin/manager)
- `is_active` - Активен ли аккаунт
- `last_login` - Время последнего входа

## 👨‍💼 Управление администраторами

### Интерактивный режим
```bash
python create_admin.py
```

### Команды CLI
```bash
# Создать администратора
python create_admin.py create

# Показать список всех администраторов
python create_admin.py list

# Сбросить пароль администратора
python create_admin.py reset
```

### Роли администраторов

**👑 Администратор (admin)**
- Полный доступ ко всем данным
- Просмотр, редактирование, удаление записей
- Управление другими администраторами
- Полный контроль над системой

**👤 Менеджер (manager)**
- Просмотр и редактирование данных
- БЕЗ доступа к управлению администраторами

## 🔔 Система уведомлений

### Принцип работы
1. Планировщик APScheduler проверяет предстоящие матчи каждые **10 минут**
2. Получает данные из внешнего API лиги
3. Вычисляет время отправки уведомлений (N часов до матча)
4. Отправляет уведомления всем пользователям с включенной подпиской
5. Сохраняет историю отправленных уведомлений в БД

### Настройка времени уведомлений
Измените переменную `NOTIFICATION_HOURS_BEFORE` в файле `.env`:
```env
NOTIFICATION_HOURS_BEFORE=2  # за 2 часа до игры
```

### Часовой пояс
Используется московское время (`Europe/Moscow`). Для изменения отредактируйте `utils/scheduler.py`.

## 🌐 API интеграция

Бот интегрируется с внешним API лиги для получения актуальных данных:

### Endpoints
- `GET /teams` - Список всех команд лиги
- `GET /games` - Список всех игр

### Кэширование
APIService кэширует ответы для уменьшения нагрузки на API. Принудительное обновление:
```python
api_service.get_teams(force_refresh=True)
api_service.get_games(force_refresh=True)
```

## 🛠️ Разработка

### Структура обработчиков

Обработчики команд организованы модульно:

```python
# handlers/start.py
def register_start_handlers(bot: TeleBot):
    @bot.message_handler(commands=['start'])
    def start_command(message: Message):
        # Обработка /start
        pass
```

### Добавление нового функционала

1. **Создайте новый handler** в `handlers/`
2. **Добавьте функцию регистрации** обработчиков
3. **Импортируйте в `bot.py`** и вызовите при инициализации
4. **Обновите модели** при необходимости в `database/models.py`
5. **Добавьте клавиатуры** в `keyboards/reply_keyboards.py`

### Работа с БД

```python
from database import get_session, User

session = get_session()
try:
    user = session.query(User).filter_by(telegram_id=123456).first()
    # работа с данными
    session.commit()
except Exception as e:
    session.rollback()
    raise e
finally:
    session.close()
```

## 📝 Логирование

Бот выводит подробные логи в консоль:
- ✅ Успешные операции
- ❌ Ошибки
- 🔍 Проверки планировщика
- 📢 Отправка уведомлений

## 🔐 Безопасность

- Пароли администраторов хранятся в виде SHA-256 хэшей
- Сессии защищены secret key
- SQL-инъекции предотвращены использованием SQLAlchemy ORM
- Аутентификация для доступа к админ-панели

**⚠️ Важно:** Измените `ADMIN_SECRET_KEY` в production!

## 🐛 Отладка

### Включение SQL логов
В `database/database.py` измените:
```python
engine = create_engine(
    config.DATABASE_URL,
    echo=True,  # Включить логирование SQL
    ...
)
```

### Проверка планировщика
Логи планировщика выводятся каждые 10 минут в консоль.

## 🚢 Деплой

### Docker (рекомендуется)
```dockerfile
# Dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

### Systemd Service
```ini
# /etc/systemd/system/hockey-bot.service
[Unit]
Description=Hockey League Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/timeofthestars_bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable hockey-bot
sudo systemctl start hockey-bot
```

## 📄 Лицензия

Этот проект создан для хоккейной лиги "Time of the Stars".

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все переменные окружения установлены
3. Проверьте доступность внешнего API
4. Убедитесь, что бот имеет все необходимые права в Telegram
