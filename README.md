# 🏒 Time of the Stars Hockey Bot

Telegram-бот для хоккейной лиги Time of the Stars.

## 📋 Функционал

- 🏒 **Просмотр матчей** - расписание игр с пагинацией (данные из API)
- 🔔 **Управление уведомлениями** - включение/выключение уведомлений о матчах
- 👥 **Управление заявками команд** - подача, редактирование и удаление заявок (множественные заявки)
- ⛸ **Управление анкетами игроков** - создание, редактирование и удаление анкет (множественные анкеты)
- 🛠 **Админ-панель** - управление всеми данными через веб-интерфейс

Подробное описание функционала см. в [FEATURES.md](FEATURES.md)

## 🏗️ Структура проекта

```
timeofthestars_bot/
├── bot.py                  # Главный файл запуска бота
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
├── .env                   # Переменные окружения
├── .env.example          # Пример .env файла
├── .gitignore            # Git ignore
├── database/             # База данных
│   ├── __init__.py
│   ├── models.py         # SQLAlchemy модели
│   └── database.py       # Подключение к БД
├── handlers/             # Обработчики команд
│   ├── __init__.py
│   ├── start.py          # /start и главное меню
│   ├── notifications.py  # Управление уведомлениями
│   ├── team_registration.py    # Регистрация команд
│   └── player_registration.py  # Регистрация игроков
├── admin/                # Админ-панель
│   ├── __init__.py
│   └── admin_panel.py    # SQLAdmin
├── keyboards/            # Клавиатуры
│   ├── __init__.py
│   └── reply_keyboards.py
└── utils/                # Утилиты
    ├── __init__.py
    └── helpers.py
```

## 🗄️ База данных

Проект использует SQLite с 4 таблицами:

1. **User** - пользователи бота (telegram_id, настройки уведомлений)
2. **TeamApplication** - заявки на регистрацию команд (множественные от одного пользователя)
3. **Player** - анкеты игроков (множественные от одного пользователя)
4. **GameNotification** - отправленные уведомления о матчах

**Важно**: Данные о командах и матчах берутся из внешнего API (`https://api.timeofthestars.ru`)

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка .env файла

Скопируйте `.env.example` в `.env` и укажите свой токен бота:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```
BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=sqlite:///hockey_league.db
ADMIN_SECRET_KEY=ваш_секретный_ключ
ADMIN_PORT=8000
API_TEAMS_URL=https://api.timeofthestars.ru/teams
API_GAMES_URL=https://api.timeofthestars.ru/games
```

### 3. Запуск бота

```bash
python bot.py
```

### 4. Запуск админ-панели (опционально)

```bash
python run_admin.py
```

Админ-панель будет доступна по адресу: `http://localhost:8000/admin`

**Важно**: Админ-панель использует Starlette + uvicorn (не Flask!)

## 🎮 Использование

### Для пользователей:

1. Отправьте `/start` боту
2. Выберите нужное действие из главного меню:
   - **🏒 Матчи** - просмотр расписания и управление уведомлениями
   - **👥 Записать команду в лигу** - создание/управление заявками команд
   - **⛸ Записаться в команду (игрок)** - создание/управление анкетами игроков

#### Управление заявками/анкетами:
- **➕ Добавить** - создать новую запись
- **✏️ Изменить** - редактировать существующую (с выбором если их несколько)
- **🗑 Удалить** - удалить запись с подтверждением
- **📋 Мои записи** - посмотреть все свои записи

### Для администраторов:

1. Откройте админ-панель в браузере (`http://localhost:8000/admin`)
2. Управляйте:
   - Пользователями и их настройками уведомлений
   - Заявками команд (просмотр, изменение статуса, добавление комментариев)
   - Анкетами игроков (просмотр, поиск по позиции/команде)
   - Историей отправленных уведомлений

## 📦 Зависимости

- `pyTelegramBotAPI==4.14.0` - работа с Telegram Bot API
- `SQLAlchemy==2.0.35` - ORM для работы с БД (совместим с Python 3.13)
- `sqladmin==0.16.1` - административная панель
- `starlette==0.27.0` - веб-фреймворк для админ-панели
- `uvicorn==0.27.0` - ASGI сервер для админ-панели
- `python-dotenv` - работа с .env файлами
- `requests` - работа с внешним API

**Важно**: Используется Starlette вместо Flask для совместимости SQLAdmin с Python 3.13

## 🔧 Конфигурация

Все настройки находятся в `config.py` и загружаются из `.env`:

- `BOT_TOKEN` - токен Telegram бота
- `DATABASE_URL` - путь к базе данных
- `ADMIN_SECRET_KEY` - секретный ключ для админ-панели
- `ADMIN_PORT` - порт для админ-панели (по умолчанию 8000)
- `API_TEAMS_URL` - URL API для получения списка команд
- `API_GAMES_URL` - URL API для получения расписания матчей

## 🎯 Особенности реализации

- **Множественные записи**: один пользователь может создать несколько заявок команд и несколько анкет игроков
- **Внешний API**: данные о командах и матчах берутся из внешнего API, не хранятся локально
- **Пагинация матчей**: просмотр расписания по 3 матча с кнопками навигации
- **Состояния регистрации**: используются словари состояний для отслеживания процесса заполнения форм
- **Подтверждение удаления**: перед удалением заявки/анкеты запрашивается подтверждение

## 📝 Лицензия

MIT License

## 👨‍💻 Разработка

Проект разработан для хоккейной лиги Time of the Stars.
